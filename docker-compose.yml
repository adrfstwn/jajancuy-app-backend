volumes:
  backend-python/django_backend/user_services:
    driver: local
volumes:
  backend-python/django_backend/user_services:
    driver: local
volumes:
  backend-python/django_backend/user_services:
    driver: local
volumes:
  backend-python/django_backend/user_services:
    driver: local
volumes:
  backend-python/django_backend/user_services:
    driver: local
